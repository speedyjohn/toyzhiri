package org.example.toy_zhiri.notification.enums;

/**
 * Типы сущностей, к которым может быть привязано уведомление.
 * Используется для формирования ссылок на фронтенде.
 */
public enum RelatedEntityType {
    BOOKING,
    SERVICE,
    REVIEW,
    SUBSCRIPTION,
    USER
}